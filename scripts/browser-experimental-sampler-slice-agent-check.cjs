const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({executablePath:process.env.PLAYWRIGHT_EXECUTABLE,headless:true});try{const page=await browser.newPage(),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.route('**/api/auth?*',r=>r.fulfill({json:{configured:true,user:{id:'zone-slice-agent',email:'test@example.com'},profile:{name:'Test',occupation:'Composer',complete:true}}}));await page.route('**/api/projects*',r=>r.fulfill({json:{projects:[]}}));await page.route('**/api/daw',r=>{if(r.request().method()==='GET')return r.fulfill({json:{configured:true}});const b=r.request().postDataJSON();assert.equal(b.allowSamplerSlicing,true);return r.fulfill({json:{revision:b.session.revision,commands:[],action:'slice_sampler_zone',options:{trackId:'t',zoneId:'z',regionStart:5,startKey:36,settings:{riseDb:9,floorDb:-45,minimumGap:.05}}}});});await page.goto((process.env.CUESTAMP_URL||'http://127.0.0.1:5190/')+'#/experimental');await page.getByText('Session restored on this device.',{exact:true}).waitFor();const {encodeWav}=await import('../src/experimental/wav.js'),pcm=Float32Array.from({length:48000},(_,i)=>(i>=14400&&i<19200)||(i>=33600&&i<38400)?.5:0),buffer=Buffer.from(await encodeWav({numberOfChannels:1,length:48000,sampleRate:48000,getChannelData:()=>pcm}).arrayBuffer());await page.locator('#daw-files').setInputFiles({name:'source.wav',mimeType:'audio/wav',buffer});await page.locator('.daw-status').filter({hasText:'Imported source.wav'}).waitFor();const read=()=>page.evaluate(()=>JSON.parse(localStorage.getItem('cuestamp-experimental:zone-slice-agent'))),asset=(await read()).tracks[0].regions[0].assetId;await page.getByText('Command harness',{exact:true}).click();await page.locator('#daw-json').fill(JSON.stringify([{op:'track.add',values:{id:'t',kind:'midi'}},{op:'region.add',target:'t',values:{id:'r',duration:1}},{op:'samplerZone.set',target:'t',values:{id:'z',name:'Trim me',assetId:asset}}]));await page.getByRole('button',{name:'Execute commands',exact:true}).click();await page.locator('[data-region=r]').click({position:{x:30,y:15}});await page.locator('[data-sampler-zones]>summary').click();const form=page.locator('[data-sampler-zone]');await form.locator('[name=zone]').selectOption('z');await page.locator('#daw-agent-form textarea').fill('Slice this sample into consecutive MIDI keys starting at 36');await page.locator('#daw-agent-form button.primary').click();await page.waitForFunction(()=>JSON.parse(localStorage.getItem('cuestamp-experimental:zone-slice-agent')).tracks.find(t=>t.id==='t').sampleZones.length===3);
const zones=(await read()).tracks.find(t=>t.id==='t').sampleZones;assert.deepEqual(zones.map(z=>[z.sourceStart,z.sourceEnd,z.root]),[[0,.3,36],[.3,.7,37],[.7,1,38]]);
assert.equal((await read()).tracks.find(t=>t.id==='t').regions.find(r=>r.id!=='r').start,5);const rendered=await page.evaluate(async()=>{const {scheduleSampler}=await import('/src/experimental/sampler.js');const {zoneSettings}=await import('/src/experimental/sampler-zones.js');const t=JSON.parse(localStorage.getItem('cuestamp-experimental:zone-slice-agent')).tracks.find(t=>t.id==='t');const result=[];for(const z of t.sampleZones){const ctx=new OfflineAudioContext(1,48000,48000),b=ctx.createBuffer(1,48000,48000),data=b.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=(i>=14400&&i<19200)||(i>=33600&&i<38400)?.5:0;const note={pitch:z.root,velocity:1,start:0,duration:1};scheduleSampler(ctx,ctx.destination,b,z.root,note,[],0,0,1,[],{...zoneSettings(t,z,note),sampleAttack:0,sampleRelease:0});const pcm=(await ctx.startRendering()).getChannelData(0);result.push([pcm[2400],pcm[7200],pcm[22000]]);}return result;});
assert.equal(rendered[0][0],0);assert.ok(rendered[1][0]>.1&&rendered[2][0]>.1);assert.equal(rendered[1][1],0);assert.equal(rendered[2][1],0);assert.ok(rendered.every(row=>row[2]===0));
await page.getByRole('button',{name:'Undo',exact:true}).click();assert.equal((await read()).tracks.find(t=>t.id==='t').sampleZones.length,1);assert.deepEqual(errors,[]);console.log('Detected slices mapped to keys, persisted, rendered correct audio portions and restored with Undo',rendered);
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
