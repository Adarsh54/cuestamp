const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');
const assert=require('node:assert/strict');const fs=require('node:fs/promises');
(async()=>{const browser=await chromium.launch({executablePath:process.env.PLAYWRIGHT_EXECUTABLE,headless:true});try{
 const page=await browser.newPage(),errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.route('**/api/auth?*',r=>r.fulfill({json:{configured:true,user:{id:'sampler-automap',email:'test@example.com'},profile:{name:'Test',occupation:'Composer',complete:true}}}));await page.route('**/api/projects*',r=>r.fulfill({json:{projects:[]}}));
 await page.route('**/api/daw',r=>{if(r.request().method()==='GET')return r.fulfill({json:{configured:true}});const b=r.request().postDataJSON();return r.fulfill({json:{revision:b.session.revision,summary:'Mapped sample roots',commands:[{op:'samplerZone.automap',target:'sampler',values:{mode:'roots',zoneIds:'["low","high"]',low:0,high:127}}]}});});
 await page.goto((process.env.CUESTAMP_URL||'http://127.0.0.1:5190/')+'#/experimental');await page.getByText('Session restored on this device.',{exact:true}).waitFor();
 const read=()=>page.evaluate(()=>JSON.parse(localStorage.getItem('cuestamp-experimental:sampler-automap')));
 const {encodeWav}=await import('../src/experimental/wav.js');for(const [name,level]of [['low',.2],['high',-.4]]){const pcm=new Float32Array(48000).fill(level),wav=Buffer.from(await encodeWav({numberOfChannels:1,length:48000,sampleRate:48000,getChannelData:()=>pcm}).arrayBuffer());await page.locator('#daw-files').setInputFiles({name:name+'.wav',mimeType:'audio/wav',buffer:wav});await page.locator('.daw-status').filter({hasText:'Imported '+name+'.wav'}).waitFor();}
 const tracks=(await read()).tracks;
 await page.getByText('Command harness',{exact:true}).click();await page.locator('#daw-json').fill(JSON.stringify([...tracks.map(t=>({op:'track.set',target:t.id,values:{mute:true}})),{op:'track.add',values:{id:'sampler',kind:'midi',sampleAttack:0,sampleRelease:0}},{op:'samplerZone.set',target:'sampler',values:{id:'low',name:'Low sample',assetId:tracks[0].regions[0].assetId,root:60}},{op:'samplerZone.set',target:'sampler',values:{id:'high',name:'High sample',assetId:tracks[1].regions[0].assetId,root:72}},{op:'region.add',target:'sampler',values:{id:'notes',duration:2}},{op:'note.add',target:'notes',values:{pitch:60,start:0,duration:.5,velocity:1}},{op:'note.add',target:'notes',values:{pitch:72,start:1,duration:.5,velocity:1}}]));await page.getByRole('button',{name:'Execute commands',exact:true}).click();await page.locator('[data-region=notes]').click({position:{x:30,y:15}});await page.locator('[data-sampler-zones]>summary').click();
 const form=page.locator('[data-sampler-automap]'),zones=async()=>(await read()).tracks.find(t=>t.id==='sampler').sampleZones;
 await form.getByRole('button',{name:'Map selected zones',exact:true}).click();assert.deepEqual((await zones()).map(z=>[z.keyLow,z.keyHigh]),[[0,66],[67,127]]);
 let pending=page.waitForEvent('download');await page.locator('[data-action=bounce]').click();const bytes=await fs.readFile(await (await pending).path()),rate=bytes.readUInt32LE(24),at=t=>bytes.readInt16LE(44+Math.round(t*rate)*4);assert.ok(at(.2)>3000);assert.ok(at(1.2)<-6000);
 await form.locator('[name=mode]').selectOption('chromatic');await form.locator('[name=low]').fill('36');await form.getByRole('button',{name:'Map selected zones',exact:true}).click();assert.deepEqual((await zones()).map(z=>[z.keyLow,z.keyHigh,z.root]),[[36,36,36],[37,37,37]]);await page.getByRole('button',{name:'Undo',exact:true}).click();assert.equal((await zones())[0].root,60);
 await form.locator('[data-map-select=none]').click();await form.locator('[name=zoneIds][value=high]').check();await form.locator('[name=mode]').selectOption('velocity');await form.locator('[name=low]').fill('80');await form.getByRole('button',{name:'Map selected zones',exact:true}).click();assert.deepEqual((await zones()).map(z=>[z.velocityLow,z.velocityHigh]),[[1,127],[80,127]]);await page.getByRole('button',{name:'Undo',exact:true}).click();await page.getByRole('button',{name:'Redo',exact:true}).click();assert.equal((await zones())[1].velocityLow,80);await page.getByRole('button',{name:'Undo',exact:true}).click();
 const before=await read();await form.locator('[name=low]').fill('100');await form.locator('[name=high]').fill('110');await form.getByRole('button',{name:'Map selected zones',exact:true}).click();await page.locator('.daw-status').filter({hasText:'include every selected root'}).waitFor();assert.deepEqual(await read(),before);
 await page.locator('#daw-instruction').fill('Spread the two samples by root pitch');await page.locator('#daw-agent-form button.primary').click();await page.waitForFunction(()=>!document.querySelector('#daw-agent-form button.primary').disabled);assert.equal((await zones())[0].keyHigh,66);
 await page.reload();await page.getByText('Session restored on this device.',{exact:true}).waitFor();assert.deepEqual((await zones()).map(z=>[z.keyLow,z.keyHigh]),[[0,66],[67,127]]);await page.locator('[data-region=notes]').click({position:{x:30,y:15}});await page.locator('[data-sampler-zones]>summary').click();await form.screenshot({path:'/tmp/cuestamp-sampler-automap.png'});assert.deepEqual(errors,[]);console.log('PASS sampler batch mapping controls, selection, atomic validation, Undo/Redo, mocked agent, reload and actual two-source rendered PCM.');
 }finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
