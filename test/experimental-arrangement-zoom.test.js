import test from 'node:test';import assert from 'node:assert/strict';import {fitArrangementRange,selectedArrangementRange} from '../src/experimental/arrangement-zoom.js';
test('range framing contains long passages with horizontal padding',()=>{for(const [start,end,width]of [[0,3600,700],[500,520,900],[80000,86400,400]]){const {zoom,left}=fitArrangementRange(start,end,width);assert.ok(start*zoom>=left);assert.ok(end*zoom<=left+width+1e-8);assert.ok(zoom>0&&zoom<=100);}assert.equal(fitArrangementRange(10,10,600).zoom,100);});
test('selection bounds include full clips across tracks without modifying the document',()=>{const s={tracks:[{regions:[{id:'a',start:5,duration:3},{id:'other',start:0,duration:1000}]},{regions:[{id:'b',start:20,duration:4}]}]},before=structuredClone(s);assert.deepEqual(selectedArrangementRange(s,['a','b']),{start:5,end:24});assert.equal(selectedArrangementRange(s,[]),null);assert.equal(selectedArrangementRange(s,['missing']),null);assert.deepEqual(s,before);});
test('invalid dimensions fail rather than producing invalid CSS',()=>{for(const args of [[-1,2,500],[10,2,500],[0,2,0],[0,Infinity,500],[0,2,NaN]])assert.throws(()=>fitArrangementRange(...args));});
