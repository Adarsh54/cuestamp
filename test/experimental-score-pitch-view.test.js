import test from 'node:test';import assert from 'node:assert/strict';import {scorePreviewDocument} from '../src/experimental/score-preview.js';import {scorePitchView} from '../src/experimental/score-transposition.js';
test('concert view exports sounding pitches without changing persistent instrument notation',()=>{const region={id:'r',name:'Theme',start:0,duration:2,notes:[{id:'n',pitch:60,start:0,duration:1,velocity:1}]},track={id:'t',kind:'midi',name:'Clarinet',scoreInstrument:'bb',regions:[region]},session={title:'Score',tempo:120,keySignature:{sharps:0,mode:'major'},tracks:[track]},before=structuredClone(session);const written=scorePreviewDocument(session,track,region,'arrangement').xml,concert=scorePreviewDocument(session,track,region,'arrangement','concert').xml;assert.match(written,/<step>D<\/step>/);assert.match(written,/<chromatic>-2/);assert.match(concert,/<step>C<\/step>/);assert.doesNotMatch(concert,/<transpose>/);assert.match(concert,/<fifths>0/);assert.deepEqual(session,before);assert.equal(scorePitchView(session,track,'concert').track.regions[0],region);assert.throws(()=>scorePitchView(session,track,'invalid'),/Choose/);});
