import test from 'node:test';import assert from 'node:assert/strict';import {musicxmlDynamics,applyMusicxmlDynamics} from '../src/experimental/musicxml-dynamics.js';
test('dynamics percentages use MIDI forte 90 and clamp at maximum velocity',()=>{assert.equal(musicxmlDynamics('100'),90/127);assert.equal(musicxmlDynamics('50'),45/127);assert.equal(musicxmlDynamics('0'),0);assert.equal(musicxmlDynamics('200'),1);for(const value of ['',null,'NaN','-1','Infinity'])assert.throws(()=>musicxmlDynamics(value),/nonnegative/);});
test('timeline dynamics apply by note onset regardless of voice/document order; note values take precedence',()=>{const notes=[{start:4,velocity:.8},{start:0,velocity:.8},{start:2,velocity:.8},{start:2,velocity:.3,explicitVelocity:true},{start:1,velocity:.8,duration:5}],points=[{start:3,velocity:.9},{start:1,velocity:.4},{start:2,velocity:.6}],before=structuredClone(points);applyMusicxmlDynamics(notes,points);assert.deepEqual(notes.map(n=>n.velocity),[.9,.8,.6,.3,.4]);assert.ok(notes.every(n=>!('explicitVelocity'in n)));assert.deepEqual(points,before);});
test('equal-time dynamics use the final event and explicit zero is retained',()=>{const notes=[{start:0,velocity:.8},{start:0,velocity:0,explicitVelocity:true}];applyMusicxmlDynamics(notes,[{start:0,velocity:.7},{start:0,velocity:.5}]);assert.deepEqual(notes.map(n=>n.velocity),[.5,0]);});
