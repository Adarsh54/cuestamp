import test from 'node:test';import assert from 'node:assert/strict';import {indexScorePosition,updateScorePosition} from '../src/experimental/score-position.js';
const element=(start,end)=>({dataset:{scoreStart:String(start),scoreEnd:String(end)},current:false,setAttribute(){this.current=true;},removeAttribute(){this.current=false;}});
test('score position follows forward motion, seeks, loops and silence without retaining stale highlights',()=>{const root={},a=element(1,2),b=element(2,3),c=element(1.5,2.5),panel={isConnected:true,querySelectorAll:()=>[a,b,c]};updateScorePosition(root,1.75);indexScorePosition(root,panel);assert.deepEqual([a.current,b.current,c.current],[true,false,true]);updateScorePosition(root,2);assert.deepEqual([a.current,b.current,c.current],[false,true,true]);updateScorePosition(root,4);assert.deepEqual([a.current,b.current,c.current],[false,false,false]);updateScorePosition(root,1.25);assert.deepEqual([a.current,b.current,c.current],[true,false,false]);updateScorePosition(root,null);assert.deepEqual([a.current,b.current,c.current],[false,false,false]);});
