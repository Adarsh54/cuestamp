import test from 'node:test';import assert from 'node:assert/strict';
import {scoreNotePlans,resolveScoreNote} from '../src/experimental/score-note-editor.js';
const fixture=()=>({tempo:120,tracks:[{id:'t',kind:'midi',regions:[{id:'r',start:1,duration:4,notes:[{id:'n',pitch:60,start:0,duration:3,velocity:1},{id:'overlap',pitch:60,start:0,duration:1,velocity:1}]}]},{id:'bass',kind:'midi',regions:[{id:'br',start:2,duration:2,notes:[{id:'bn',pitch:48,start:0,duration:1,velocity:1}]}]}]});
test('score lookup distinguishes voices, ties and local versus arrangement time',()=>{const s=fixture(),t=s.tracks[0],r=t.regions[0],before=structuredClone(s),region=scoreNotePlans(s,t,r,'region')[0];assert.deepEqual(resolveScoreNote(region,{pitch:60,tick:3840,voice:1}),{noteId:'n',regionId:'r'});assert.deepEqual(resolveScoreNote(region,{pitch:60,tick:0,voice:2}),{noteId:'overlap',regionId:'r'});const parts=scoreNotePlans(s,null,null,'arrangement');assert.equal(resolveScoreNote(parts[0],{pitch:60,tick:0,voice:1}),null);assert.deepEqual(resolveScoreNote(parts[1],{pitch:48,tick:3840,voice:1}),{noteId:'bn',regionId:'br'});assert.equal(resolveScoreNote(parts[1],{pitch:48,tick:5760,voice:1}),null);assert.deepEqual(s,before);});
