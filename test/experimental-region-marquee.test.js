import test from 'node:test';import assert from 'node:assert/strict';import {marqueeRegions,combinedRegionSelection} from '../src/experimental/region-marquee.js';
const regions=[{id:'audio',left:20,right:80,top:36,bottom:100},{id:'midi',left:50,right:100,top:124,bottom:188},{id:'video',left:150,right:210,top:212,bottom:276}];
test('arrangement marquee intersects visible rectangles in either direction across tracks',()=>{assert.deepEqual(marqueeRegions(regions,{x:30,y:50},{x:110,y:200}),['audio','midi']);assert.deepEqual(marqueeRegions(regions,{x:110,y:200},{x:30,y:50}),['audio','midi']);assert.deepEqual(marqueeRegions(regions,{x:100,y:36},{x:150,y:280}),[]);assert.deepEqual(marqueeRegions(regions,{x:149,y:211},{x:151,y:213}),['video']);assert.deepEqual(marqueeRegions(regions,{x:0,y:0},{x:1,y:1}),[]);});
test('additive selection deduplicates, replacement clears, and overflow rejects without truncation',()=>{assert.deepEqual(combinedRegionSelection(['audio'],['audio','midi'],true),['audio','midi']);assert.deepEqual(combinedRegionSelection(['audio'],[],true),['audio']);assert.deepEqual(combinedRegionSelection(['audio'],[],false),[]);const previous=Array.from({length:1000},(_,i)=>'r'+i);assert.throws(()=>combinedRegionSelection(previous,['overflow'],true),/1,000/);assert.equal(previous.length,1000);});
