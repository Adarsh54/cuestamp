import {trackColorStyle} from './track-color.js';
import {compressorMeterView} from './compressor-meter.js';
import {eqTypes} from './eq-modes.js';
import {effectPresetsView,bindEffectPresets} from './effect-presets.js';
import {channelAllocationView,bindChannelAllocation} from './midi-channel-allocation.js';
import {pitchBendView,bindPitchBend} from './pitch-bend.js';
import {effectLiveControls} from './effect-live-controls.js';
import {trackHierarchy,groupFoldButton,revealTrackCommands} from './track-hierarchy.js';
import {summingGroupView,bindSummingGroup,groupMembersView} from './summing-group.js';
import {channelSettingsView,bindChannelSettings} from './channel-settings.js';
import {eqGraphView,bindEqGraphs} from './eq-graph.js';
import {effectParameters} from './effect-automation.js';
import {meterView} from './meters.js';
import {routingView,bindRouting} from './routing.js';
import {automationView,bindAutomation} from './automation-editor.js';
function effectChainView(track,esc,master=false,touchMode='static'){return `<section><div class="daw-effect-toolbar"><h4>${esc(track.name)} · Effect chain</h4><select data-new-effect aria-label="Effect type"><option value="eq">EQ</option><option value="gain">Gain / Stereo utility</option><option value="compressor">Compressor</option><option value="delay">Delay</option><option value="reverb">Reverb</option><option value="tremolo">Stereo tremolo</option><option value="chorus">Stereo chorus</option><option value="phaser">Stereo phaser</option><option value="distortion">Distortion</option></select><button data-add-effect="${track.id}">Add effect</button></div>${(track.effects||[]).map((e,i)=>`<form class="daw-effect" data-effect-form="${e.id}"><div class="section-title"><strong>${i+1}. ${esc(e.kind)}</strong><label><input name="enabled" type="checkbox" ${e.enabled?'checked':''}> Enabled</label><button type="button" data-effect-up="${e.id}" data-index="${i}" ${i===0?'disabled':''}>↑</button><button type="button" data-effect-remove="${e.id}">Remove</button></div><div class="daw-effect-fields">${Object.entries(e).filter(([k])=>!['id','kind','enabled','automation','automationMode','automationMuted'].includes(k)).map(([key,value])=>typeof value==='boolean'?`<label><input type="checkbox" name="${key}" ${value?'checked':''}>${esc(({invertLeft:'Invert left polarity',invertRight:'Invert right polarity',swap:'Swap left / right',sync:'Sync to project tempo'})[key]||key)}</label>`:typeof value==='number'?`<label>${esc(effectParameters[e.kind]?.[key]?.label||({phase:'Start phase · degrees',stereoPhase:'Stereo phase · degrees',beats:'Sync cycle · quarter-note beats'})[key]||key)}<input type="number" step="any" name="${key}" value="${value}" required></label>`:`<label>${esc(key)}<select name="${key}">${eqTypes.map(v=>`<option ${value===v?'selected':''}>${v}</option>`).join('')}</select></label>`).join('')}</div>${eqGraphView(e)}${compressorMeterView(e)}<button type="submit">Apply effect</button></form>${effectLiveControls(e,esc,touchMode)}<details data-effect-automation="${e.id}"><summary>Automate ${esc(e.kind)}</summary>${automationView({...e,name:e.kind},esc,{parameters:effectParameters[e.kind],inheritedOff:track.automationMode==='off'})}<small>Times are in project seconds. Curves override static values. Reverb decay, filter type, polarity, channel swap, stereo phase, modulation sync settings and bypass remain static. ${e.kind==='compressor'?'Makeup gain adjusts the compressed output level and can be automated. Zero adds no extra gain. The browser compressor has its own fixed gain compensation; this control does not disable it or provide automatic loudness matching.':''} ${e.kind==='distortion'?'Symmetric soft clipping with 4× oversampling, followed by a low-pass tone filter. Drive adds saturation; Wet output sets processed level without changing the dry signal. Mix 0 is dry. All four controls can be automated. This is not automatic loudness compensation.':''} ${e.kind==='phaser'?'Four all-pass stages per channel with a sine sweep. Mix 0 is dry; Mix 0.5 produces the strongest cancellation notches. Depth 0 fixes the filters. Stereo phase offsets the right sweep. Tempo sync uses quarter-note beats per cycle and ignores retained free-rate values and automation. Seeking restores oscillator phase but starts with empty filter history. Center frequency is capped at one eighth of the sample rate to keep the full sweep below Nyquist.':''} ${e.kind==='chorus'?'One sine-modulated delay per channel, centered at 25 ms. Depth 0 retains a fixed delay; Mix 0 is dry. Stereo phase offsets the right modulation. Rate, depth and mix can be automated. Tempo sync uses quarter-note beats per cycle and ignores retained free-rate automation. Seeking restores modulation phase, but delay history starts empty.':''} ${e.kind==='tremolo'?'Sine volume modulation. Depth 0 leaves levels unchanged; 1 reaches silence. Stereo phase 180 alternates sides. Tempo sync uses beats per cycle and ignores free-rate values and automation; depth automation stays active. Modulation follows project time, including seeks.':''} ${e.kind==='gain'?'Width 0 averages left/right to mono; 1 preserves stereo; 2 doubles the side signal. Polarity is applied before width; channel swap afterward.':''}</small></details>`).join('')||`<p class="muted">Insert effects run in order, before the ${master?'master':'track'} fader.</p>`}</section>`;}
const masterChannel=session=>({id:session.id,name:'Master',gainDb:session.masterDb,pan:session.masterPan||0,effects:session.masterEffects||[],automation:session.masterAutomation||[],automationMode:session.masterAutomationMode,automationMuted:session.masterAutomationMuted});
export function mixerView(session,selected,esc,{touchMode='static',touchCount=0,liveValue=()=>undefined}={}){
 const trimMode=touchMode.startsWith('trim');
 const rows=trackHierarchy(session).filter(r=>r.track.kind!=='video'),tracks=rows.map(r=>r.track),track=session.tracks.find(t=>t.id===selected)||tracks[0],masterSelected=selected===session.id,master=masterChannel(session);
 const strip=({track:t,depth,children})=>`<div data-track-color="${esc(t.color||'')}" style="${trackColorStyle(t)}" class="daw-channel ${!masterSelected&&track?.id===t.id?'selected':''}">${groupFoldButton(t,children.length,esc)}<button data-mix-select="${t.id}" style="padding-left:${Math.min(depth,4)*8}px">${esc(t.name)}</button>${meterView(t.id,t.name,esc)}<label>${trimMode?'Volume trim':'Volume'} <output>${(liveValue(t.id,'gainDb')??t.gainDb).toFixed(1)} dB</output><input type="range" data-mix-gain="${t.id}" min="-96" max="12" step=".5" value="${liveValue(t.id,'gainDb')??t.gainDb}" aria-label="${esc(t.name)} volume"></label><label>${trimMode?'Pan trim':'Pan'} <output>${(liveValue(t.id,'pan')??t.pan).toFixed(2)}</output><input type="range" data-mix-pan="${t.id}" min="-1" max="1" step=".05" value="${liveValue(t.id,'pan')??t.pan}" aria-label="${esc(t.name)} pan"></label><span class="muted">${t.effects?.length||0} effects · ${t.automation?.length||0} automation points</span></div>`;
 return `<section class="daw-mixer"><div class="section-title"><h3>Mixer</h3><label>Fader mode<select data-touch-mode><option value="static" ${touchMode==='static'?'selected':''}>Static</option><option value="touch" ${touchMode==='touch'?'selected':''}>Touch recording</option><option value="latch" ${touchMode==='latch'?'selected':''}>Latch recording</option><option value="write" ${touchMode==='write'?'selected':''}>Write selected channel</option><option value="trimTouch" ${touchMode==='trimTouch'?'selected':''}>Trim Touch</option><option value="trimLatch" ${touchMode==='trimLatch'?'selected':''}>Trim Latch</option></select></label><span data-touch-status aria-live="polite">${touchCount?`${touchCount} automation lane${touchCount===1?'':'s'} recording`: ''}</span>${touchCount?'<button type="button" data-touch-cancel>Discard automation pass</button>':''}<button data-mix-select="${session.id}" aria-pressed="${masterSelected}">Master channel · ${(session.masterEffects||[]).length} effects</button><label>Master${trimMode?' trim':''} · dB<input type="number" data-master-gain min="-96" max="12" step=".5" value="${liveValue(session.id,'gainDb')??session.masterDb}"></label><label>Master${trimMode?' trim':''} · pan<input type="number" data-master-pan min="-1" max="1" step=".05" value="${liveValue(session.id,'pan')??session.masterPan??0}"></label></div>${meterView(session.id,'Master',esc)}<small class="muted">Touch returns to the saved curve on release. Latch holds every moved fader until Stop. Write replaces volume and pan from playback start on the selected channel, then switches to Touch on Stop. Trim controls start at zero and offset the moving curve (volume ±12 dB, pan ±1). Start playback to record Trim. Offsets must fit the remaining curve’s limits. Recording requires Cycle off; Stop saves, Escape or Discard cancels the active pass. Live sample peaks after volume and pan. Cycle shows the combined master output only. Peaks reset on playback restart.</small>${channelAllocationView(session,esc)}${summingGroupView(session,esc)}<div class="daw-channels">${rows.map(strip).join('')||'<p class="muted">Add audio or instrument tracks to mix.</p>'}</div>${masterSelected?`<div class="daw-mix-detail">${effectPresetsView(session,esc)}${effectChainView(master,esc,true,touchMode)}${automationView(master,esc)}</div><p class="muted daw-master-description">Processes the complete mix before master volume. Master effects also process each stem individually; compression can make the summed stems differ from the full mix. Volume and pan curves override the master controls.</p>`:track?`${groupMembersView(session,track,esc)}${channelSettingsView(session,track,esc)}${pitchBendView(track)}${routingView(session,track,esc)}<div class="daw-mix-detail">${effectPresetsView(session,esc)}${effectChainView(track,esc,false,touchMode)}${automationView(track,esc)}</div>`:''}</section>`;
}
const effectParameterSelections=new Map();
export function bindMixer(root,{session,selected,select,execute,guard,duration,touch=null,touchMode='static',onTouchMode=()=>{},cancelTouch=()=>{},isPlaying=()=>false,afterTouch=()=>{},automationParameter='gainDb',onAutomationParameter=()=>{},sampleRate=48000,blocked=()=>false}){
 bindChannelAllocation(root,{execute,guard});
 bindSummingGroup(root,{session,execute,guard,blocked,select});
 root.querySelectorAll('[data-mix-fold]').forEach(el=>el.onclick=guard(()=>{const t=session.tracks.find(t=>t.id===el.dataset.mixFold);execute([{op:'track.set',target:t.id,values:{collapsed:!t.collapsed}}],t.collapsed?'Expanded group':'Collapsed group');select(t.id);}));
 root.querySelectorAll('[data-mix-select]').forEach(el=>el.onclick=guard(()=>{const commands=revealTrackCommands(session,el.dataset.mixSelect);if(commands.length)execute(commands,'Revealed group member');select(el.dataset.mixSelect);}));
 root.querySelector('[data-touch-mode]')?.addEventListener('change',guard(e=>onTouchMode(e.target.value)));
 root.querySelector('[data-touch-cancel]')?.addEventListener('click',cancelTouch);
 function bindFader(input,target,parameter,staticCommand,busId,spec){
  const trimMode=touchMode.startsWith('trim');
  if(trimMode){input.dataset.trimOffset='true';input.min=spec?String(-(spec.max-spec.min)):parameter==='gainDb'?'-12':'-1';input.max=spec?String(spec.max-spec.min):parameter==='gainDb'?'12':'1';input.setAttribute('aria-label',(input.getAttribute('aria-label')||'Master '+parameter)+' trim offset');}
  if(busId!==undefined){input.dataset.sendSource=target;input.setAttribute('aria-label',trimMode?'Send trim offset':'Send volume');if(trimMode)input.parentElement.firstChild.textContent='Send trim ';}
  let recording=false,lastAccepted=input.value;
  const readValue=()=>{
   if(!input.value.trim()||input.validity?.badInput)return null;
   const value=Number(input.value);
   if(!Number.isFinite(value)||(input.min!==''&&value<Number(input.min))||(input.max!==''&&value>Number(input.max)))return null;
   return value;
  };
  input.onpointerdown=()=>{input.dataset.mixerEditing='true';};
  input.onpointerup=()=>{delete input.dataset.mixerEditing;};
  input.onlostpointercapture=()=>{delete input.dataset.mixerEditing;};
  input.oninput=guard(()=>{
   input.dataset.mixerEditing='true';
   const value=readValue();if(value===null)return;
   if(trimMode&&!isPlaying())throw Error('Start playback before recording a trim offset.');
   const output=input.parentElement.querySelector('output');if(output)output.textContent=input.value+(parameter==='gainDb'?' dB':'');
   if(touchMode!=='static'&&isPlaying()){
    if(blocked())throw Error('Finish the current operation before recording automation.');
    touch.input(target,parameter,value,busId);recording=true;lastAccepted=input.value;
   }
  });
  const finish=()=>{if(!recording)return false;recording=false;touch.release(target,parameter,busId);afterTouch();return true;};
  input.onchange=guard(()=>{delete input.dataset.mixerEditing;const value=readValue();if(value===null){input.value=lastAccepted;finish();return;}if(!finish()){if(trimMode){afterTouch();return;}execute([staticCommand(value)],'Updated mixer');lastAccepted=input.value;}});
  input.onblur=guard(()=>{delete input.dataset.mixerEditing;if(readValue()===null)input.value=lastAccepted;return finish();});
  input.onpointercancel=()=>{delete input.dataset.mixerEditing;if(recording){recording=false;touch.cancel();afterTouch();}};
  input.onkeydown=guard(e=>{if(e.key==='Escape'&&(recording||touch?.active)){e.preventDefault();e.stopPropagation();delete input.dataset.mixerEditing;recording=false;touch.cancel();afterTouch();}});
 }
 for(const [attr,parameter]of [['gain','gainDb'],['pan','pan']])root.querySelectorAll(`[data-mix-${attr}]`).forEach(input=>{
  const target=input.dataset[attr==='gain'?'mixGain':'mixPan'];bindFader(input,target,parameter,value=>({op:'track.set',target,values:{[parameter]:value}}));
 });
 for(const [attr,parameter,field]of [['gain','gainDb','masterDb'],['pan','pan','masterPan']]){
  const input=root.querySelector(`[data-master-${attr}]`);if(input)bindFader(input,session.id,parameter,value=>({op:'session.set',values:{[field]:value}}));
 }
 root.querySelectorAll('[data-effect-live]').forEach(input=>{
  const target=input.dataset.effectLive,parameter=input.dataset.effectParameter,effect=[...(session.masterEffects||[]),...session.tracks.flatMap(t=>t.effects||[])].find(e=>e.id===target);
  bindFader(input,target,parameter,value=>({op:'effect.set',target,values:{[parameter]:value}}),undefined,effectParameters[effect.kind][parameter]);
 });
 root.querySelector('[data-add-effect]')?.addEventListener('click',guard(e=>execute([{op:'effect.add',target:e.target.dataset.addEffect,values:{kind:root.querySelector('[data-new-effect]').value}}],'Added effect')));
 root.querySelectorAll('[data-effect-form]').forEach(form=>form.onsubmit=guard(e=>{e.preventDefault();const values={};for(const input of form.querySelectorAll('[name]'))values[input.name]=input.type==='checkbox'?input.checked:input.type==='number'?Number(input.value):input.value;execute([{op:'effect.set',target:form.dataset.effectForm,values}],'Updated effect');}));
 root.querySelectorAll('[data-effect-remove]').forEach(button=>button.onclick=guard(()=>execute([{op:'effect.delete',target:button.dataset.effectRemove}],'Removed effect')));
 root.querySelectorAll('[data-effect-up]').forEach(button=>button.onclick=guard(()=>execute([{op:'effect.move',target:button.dataset.effectUp,values:{index:Number(button.dataset.index)-1}}],'Reordered effects')));
 const tracks=session.tracks.filter(t=>t.kind!=='video'),track=selected===session.id?masterChannel(session):tracks.find(t=>t.id===selected)||tracks[0];if(!track)return;
 bindEffectPresets(root,{session,target:track.id,execute,guard,blocked});
 bindPitchBend(root,{track,execute,guard});
 if(track.id!==session.id)bindChannelSettings(root,{session,track,execute,guard,blocked});
 bindEqGraphs(root,{effects:track.effects,execute,guard,sampleRate,blocked,revision:session.revision});
 for(const effect of track.effects){const scope=root.querySelector(`[data-effect-automation="${effect.id}"]`);bindAutomation(scope,{track:effect,session,execute,guard,duration,parameters:effectParameters[effect.kind],mode:value=>({op:'effect.set',target:effect.id,values:{automationMode:value}}),automationParameter:effectParameterSelections.get(effect.id)||Object.keys(effectParameters[effect.kind])[0],onAutomationParameter:key=>effectParameterSelections.set(effect.id,key),point:values=>({op:'effect.automation.point',target:effect.id,values}),edit:(id,values)=>({op:'effect.automation.set',target:id,values}),remove:id=>({op:'effect.automation.delete',target:id}),clear:parameter=>({op:'effect.automation.clear',target:effect.id,values:{parameter}})});}
 if(track.id!==session.id)bindRouting(root,{track,session,execute,guard,duration,bindSendFader:(input,busId)=>bindFader(input,track.id,'gainDb',value=>({op:'send.set',target:track.id,values:{busId,gainDb:value}}),busId)});
 bindAutomation(root.querySelector('.daw-mix-detail > .daw-automation'),{track,session,execute,guard,duration,mode:value=>track.id===session.id?{op:'session.set',values:{masterAutomationMode:value}}:{op:'track.set',target:track.id,values:{automationMode:value}},automationParameter,onAutomationParameter});
}
